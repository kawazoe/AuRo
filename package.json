{
  "name": "AuRo",
  "description": "Audio Router - Pick audio output device for each tab and remember them for later.",
  "author": "Etienne Maheu",
  "version": "0.3.0",
  "type": "module",
  "scripts": {
    "build": "rollup -c rollup.config.js",
    "watch": "rollup -c rollup.config.js -w --profile=verbose"
  },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^16.0.1",
    "@rollup/plugin-replace": "^6.0.2",
    "rollup": "^4.41.0",
    "rollup-plugin-chrome-extension": "^3.6.12",
    "rollup-plugin-copy2": "^0.4.0",
    "rollup-plugin-empty-dir": "^1.0.5",
    "rollup-plugin-zip": "^1.0.3"
  },
  "dependencies": {
    "webextension-polyfill": "^0.12.0"
  },
  "overrides": {
    "rollup-plugin-empty-dir": {
      "rollup": "^4.0.0"
    }
  }
}
